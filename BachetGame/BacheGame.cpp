#include <vector>
#include <algorithm>
#include <iostream>
using namespace std;

int main(){
    int stones;
    int choicesNum;
    cin >> stones;
    cin >> choicesNum;
    vector<int> choices;
    size_t len = 0;
    while (cin >> choices[len]){
        len++;
    }
    // stan moves first

    return 0;
}